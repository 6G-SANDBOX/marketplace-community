---
name: "Robot Framework Test Appliance"
version: "autofill"
software_version: "v1.0.0"
publisher: Telefonica Innovación Digital
description: |-
  This appliance contains a Robot Framework docker image ready to launch functional and performance tests over selected VMs deployed inside OpenNebula to generate a report.

  After deploying the appliance, check the status of the deployment in /etc/one-appliance/status. You chan check the appliance logs in /var/log/one-appliance/.
  
  The appliance configuration can be customized via parameters:

  * `ONEAPP_ROBOT_PUB_SSH_KEY`  - Base64-encoded SSH public key to be present on SUT VMs.
  * `ONEAPP_ROBOT_PRIV_SSH_KEY` - Base64-encoded SSH private key used by Robot to connect SUT VMs.

  **Note**: Remember to add a password variable in the template in order to have access to it, or make sure that your OpenNebula user has an SSH Public key preconfigured.

short_description: Robot Framework Test Appliance
tags:
  - TID
  - Robot
  - Robot Framework
  - iperf3 server
  - ubuntu
format: qcow2
creation_time: "autofill"
os-id: Ubuntu
os-release: 22.04 LTS
os-arch: x86_64
hypervisor: KVM
opennebula_version: >-
  6.6, 6.8, 6.10, 7.0
opennebula_template:
  context:
    network: 'YES'
    set_hostname: "$NAME"
    ssh_public_key: "$USER[SSH_PUBLIC_KEY]"
    netcfg_type: "interfaces"
    token: 'YES'
    oneapp_robot_pub_ssh_key: "$ONEAPP_ROBOT_PUB_SSH_KEY"
    oneapp_robot_priv_ssh_key: "$ONEAPP_ROBOT_PRIV_SSH_KEY"
  cpu: '2'
  vcpu: '2'
  cpu_model:
    model: "host-passthrough"
  graphics:
    listen: 0.0.0.0
    type: vnc
  hypervisor: kvm
  memory: '4096'
  memory_unit_cost: 'MB'
  os:
    arch: x86_64
  logo: images/logos/ubuntu.png
  user_inputs:
    oneapp_robot_pub_ssh_key:   "O|text|ONEAPP_ROBOT_PUB_SSH_KEY: Base64-encoded SSH public key to be present on SUT VMs."
    oneapp_robot_priv_ssh_key:  "O|password|ONEAPP_ROBOT_PRIV_SSH_KEY: Base64-encoded SSH private key used by Robot to connect SUT VMs."
logo: robot.png
images:
  - name: "autofill"
    url: "autofill"
    type: OS
    dev_prefix: vd
    driver: qcow2
    size: "autofill"
    checksum:
      md5: "autofill"
      sha256: "autofill"
